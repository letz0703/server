{
  "name": "notion-sdk-project",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start_old": "ts-node src/index.ts",
    "start": "tsc && node dist/index.js",
    "typecheck": "tsc --noEmit",
    "build": " tsc",
    "deploy": "gh-pages -d build",
    "format": "prettier --write src/**/*.ts"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "@notionhq/client": "^2.0.0",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^18.11.9",
    "prettier": "^2.7.1",
    "ts-node": "^10.8.2",
    "typescript": "^4.7.4"
  }
}
